# hardware-configuration.nix 有意保持 git 跟踪（flake 只读取已跟踪文件），见 AGENTS.md

# nix build 产物符号链接（nh os build / nixos-rebuild build 的默认输出）
result
result-*

# codegraph 图索引（由 codegraph init 从源码生成）
.codegraph/

# AI 工具的本地配置/状态（含密钥或会话状态，不入库）
.kimi-code/
.pi/
.mcp.json
opencode.json

# 常见兜底规则
.env*
*.bak
*.swp
*.swo
